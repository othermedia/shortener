--
source_directory:   source
build_directory:    build
layout:             together
builds:
  min:
    shrink_vars:    true
    private:        true
packages:
  shorten:
    files:
      - shorten
    meta:
      provides:
        - Shortener
      requires:
        - JS.Singleton
        - BitlyClient
        - BitlyCB
