--
source_directory:   .
build_directory:    .
layout:             together
builds:
  min:
    shrink_vars:    true
    private:        true
packages:
  shorten:
    files:
      - shorten
    meta:
      provides:
        - Shortener
      requires:
        - JS.Singleton
        - BitlyClient
        - BitlyCB

